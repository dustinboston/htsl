{
  "name": "html-css",
  "module": "src/index.ts",
  "type": "module",
  "bin": {
    "html-css": "dist/cli.js"
  },
  "scripts": {
    "build": "bun build ./src/cli.ts --outdir dist --format esm",
    "start": "bun ./server.ts",
    "dev": "bun run build && bun run start",
    "prepublishOnly": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^17.2.0",
    "postcss": "^8.5.6"
  }
}
